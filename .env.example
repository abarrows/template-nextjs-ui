# Environment variable files:
#   Secrets-example.json - sample variables for each KeyVault
#   Secrets.json - variables for each KeyVault
#   .env - generated by `yarn keys:get` with variables for the selected KeyVault
#   .env.development - override non-secret values from .env while developing the project locally
#   .env.production - override non-secret values from .env while building the project locally
#   .env.*.local - override secrets for local use only

# Environment variable load order is:
#   1. process.env
#   2. .env.$(NODE_ENV).local
#   3. .env.local (Not checked when NODE_ENV is test.)
#   4. .env.$(NODE_ENV)
#   5. .env

# Reference:
# https://nextjs.org/docs/app/building-your-application/configuring/environment-variables